I need to build a windows app. This app is going to be a repo for links that I want to share with my students during class. The app should allow the user to add days and store the configuration locally. The days should selectable from a hamburger menu. 

Once on a selected day, the user should be able to create sections for that day. Under the sections the users can create multiple hyper links. The "title" of each link should be automatically fetched from the site itself when created. The user should be able to edit this title. Each link should have two buttons on the right side. 

One button opens the link in chrome. 
The second link focuses a window and pastes the link. This should work based on the window title and be configurable globally by the user. 

The app should have a feature to navigate to all of the links in the system and report broken links. There should be a setting that forces the app to check all links on startuo of the application. 
There should be a focus on making the app compact with limited padding to allow it to run in a smaller window size. 
The theme of this app should be a fun 8 bit with the ability to create a custom theme.   
